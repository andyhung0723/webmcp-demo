# Copy to .env and fill in. The backend agent (server/index.mjs) reads these.
# Without ANTHROPIC_API_KEY the agent runs in scripted mode (bridge proof, no LLM).
ANTHROPIC_API_KEY=

# Optional overrides:
# AGENT_MODEL=claude-sonnet-4-6
# PORT=5174
